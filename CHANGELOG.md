# Changelog

本项目所有值得注意的变更都会记录在此文件。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-09-02

### 新增
- **Canvas 表格脱敏**：对 canvas 渲染的表格（如飞书电子表格）截图并 OCR 识别后，在 canvas 上方用覆盖层打码。解决此类表格 DOM 无文本节点、传统「文本 / HTML 表格 / 图片」三条脱敏路径均失效的盲区。
- 配置页新增「Canvas 表格脱敏」独立开关（默认关闭），以及可配置的最小尺寸阈值（宽 × 高，默认 200×200）；打开开关时自动展开阈值输入。
- canvas 打码支持周期重扫（2 秒）+ 滚动 / 滚轮 / 点击事件防抖触发，跟随表格滚动实时更新。

### 说明
- canvas 打码仅使用「全文」规则的识别规则（正则 / 词典）；「列名 / 列值」规则对 canvas 无列语义、不适用。
- canvas 打码为真打码（无悬停显原文），覆盖层点击穿透（`pointer-events: none`），不影响表格交互。
- 跨域污染的 canvas（tainted）无法读取位图，将自动跳过。

## [1.0.0] - 2026-09-02

### 新增
- 首个正式版本。
- 文本脱敏：正文与 HTML 表格，保留首尾 N 字符、中间灰底遮罩，悬停显示原文。
- 图片 OCR 打码：本地 WASM OCR 识别图片内文字后真打码。
- 按站点开关（工具栏 popup）、URL 候选集、正则 / 词典识别规则、JSON 配置导入导出。
- 中英双语界面，以及随浏览器语言切换的扩展名称 / 描述（`_locales`）。
